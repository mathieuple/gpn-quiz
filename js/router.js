export function router(render){function update(){const route=location.hash.slice(1)||'/',active=route.startsWith('/course')?'/courses':route.startsWith('/notion')?'/notions':route;document.body.dataset.route=active;render(route);document.querySelectorAll('[data-route]').forEach(a=>{if(a.dataset.route===active)a.setAttribute('aria-current','page');else a.removeAttribute('aria-current');});document.querySelector('#main').focus({preventScroll:true});window.scrollTo(0,0);}window.addEventListener('hashchange',update);update();return update;}
